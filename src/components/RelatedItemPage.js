import React from 'react';

const RelatedItemPage = ({engineType}) => {
    return (
        <div>
            <h1>Related Item Page {engineType}</h1>
        </div>
    );
};

export default RelatedItemPage;